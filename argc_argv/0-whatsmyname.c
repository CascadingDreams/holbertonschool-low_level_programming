#include <stdio.h>

/**
 * main - entry point
 * @argc: count
 * @argv: vector
 *
 * Return: Always 0 (Success)
 */
int main(int argc, char *argv[])
{
	if (argc == 1)
	printf("%s\n", *argv);
	return (0);
}

#include "main.h"
#include <stdio.h>

/**
 *
 *
 *
 *
 */
void print_diagsums(int *a, int size)
{
	int rowCount = 0;
	int colCount = 0;

	while (a[rowCount] < size)
	{
		rowCount++;
		while (a[colCount] < size)
		{
			//colCount++;
		}
		if (a[rowCount] == a[colCount])
		{
			sum = 
			sum++;
		}
	}

			// primary diagonal 
			// if row == col
			// r == n - 1
	

}
